import { Box, Typography } from '@mui/material';

function Header() {
  return (
    <Box className="text-center p-12">
      <Typography variant="h4">Listing Products</Typography>
    </Box>
  );
}

export default Header;
